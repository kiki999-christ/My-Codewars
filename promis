/*let lists= ['Queen','Christ','Amitie']
function checkPoint(name){
    return new Promise ((resolve, reject) => {
    setTimeout (() => {
        lists.includes(name) ? resolve(`${name} You are One of us`) : reject(`${name} You are not One of us`);
        
    },2000)
})
}
checkPoint('Eric').then((message) => console.log(`I knew , ${message}`)).catch((message)=> console.log(`I don't know you ${message}`));
*/




/*const celebrity = {
  status: "looking for work"
};
const celebrity = {
    name:
}
const agent = new Proxy(celebrity, {
  get(target, propName) {
    return `Sorry, Richard's ${propName} is private right now.`;
  }
});


console.log(agent.status);
*/

const mama= { available : "not available"};
const handler = { get(target, propName){
    return `Sorry, Mama's is not ${propName} private right now.`;

}}
const  christ = new Proxy(mama, handler);
console.log(christ.available);